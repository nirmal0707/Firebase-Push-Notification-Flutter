class NotificationModel {
  //Add notification model below
  String title; //Push notification title.
  String body; //Push notification body.

  //Add data model below
  String message; //This is the message(or main content).

  NotificationModel({this.title, this.body, this.message});
}
